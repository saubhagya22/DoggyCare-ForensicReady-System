# 🐶 Doggy Care – Secure & Forensic-Ready Smart Digital Solution

## Overview
Doggy Care is a secure, forensic-ready pet care management platform that centralizes pet health, grooming and activity tracking.  
This repository is a scaffold (backend + frontend) based on the final project proposal.

## Structure
- `backend/` — Spring Boot (Java) service skeleton
- `frontend/` — React app skeleton
- `database/` — sample SQL schema & forensic log table
- `security/` — policies & forensic-readiness notes
- `docs/` — diagrams and project proposal
- `tests/` — testing plans & results placeholders

## Quick start (local)
1. Backend:
   ```bash
   cd backend
   mvn spring-boot:run
   ```
2. Frontend:
   ```bash
   cd frontend
   npm install
   npm start
   ```

## License
MIT
